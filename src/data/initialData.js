/**
 * DayFlow Initial Schedule Dataset
 * Returns empty slots object by default so un-entered time slots remain blank.
 */

export function getPresetInitialSlots(dates) {
  // Slots remain completely blank until explicitly entered by the user
  return {};
}
